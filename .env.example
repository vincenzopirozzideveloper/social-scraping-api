# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=instagram_db  # Use the same name as in your .env file

# Instagram Credentials (optional - can use credentials.json instead)
IG_EMAIL=your_email@example.com
IG_PASSWORD=your_password

# Environment Settings
IS_DOCKER=false
HEADLESS_MODE=false